{
  "name": "campus-notes-ai",
  "version": "1.0.0",
  "description": "Production-quality College Notes Sharing and AI Study Platform",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:recommendation": "python -m uvicorn recommendation.api.main:app --host 127.0.0.1 --port 8000",
    "dev": "concurrently -n \"backend,frontend,recommendation\" -c \"blue,green,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:recommendation\"",
    "dev:all": "concurrently -n \"backend,frontend,recommendation\" -c \"blue,green,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:recommendation\"",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
